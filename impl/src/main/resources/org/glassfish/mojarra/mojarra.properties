#
# Token-substituted by maven-resources-plugin at build time. Single source of truth for the
# Mojarra version string consumed at runtime by org.glassfish.mojarra.util.MojarraVersion.
#
version=${project.version}
